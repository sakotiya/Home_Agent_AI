services:
  redis:
    image: redis:7-alpine
    container_name: relocateiq-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - relocateiq_redis_data:/data

volumes:
  relocateiq_redis_data:
